.container-fluid
  .card.my-2
    = render 'shared/card/card_header', title: 'Nouvelle Catégorie'
    .card-body
      = simple_form_for @category, url: category_index_path, data: {turbo: false} do |f|
        = render 'form', f: f
